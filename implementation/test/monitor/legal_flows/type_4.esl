function main () {
  h:= false;
  l := true;
  oh := {};
  ol := {};
  ol.p := false;
  x:= "upgVar"("l", "low");
  x:= "upgVar"("h", "high");
  x:= "upgStruct"(ol, "low");
  x:= "upgStruct"(oh, "high");

  if (h) {
    oh := ol
  };
  oh.p := true;
  x:=oh.p;
  if (!x) {
    l := false
  };
  return l

}
