function main() {
	x := extern str_symbol("x");
  	y := s_len(x);
	se_assume(y = 4);

	se_assume(s_substr(x, 0, 3) = "EFG");
	se_assume(s_nth(x, 3) = "A");

	z := s_substr(x, 0, 3);
	assert(z = "EFG");

	return 1
}
