function main() {
	x := symbolic(__$Str, "x");
  	y := s_len(x);
	assume(y > 3);
	assume(y < 5);

	assume(s_substr(x, 0, 3) = "EFG");
	assume(s_nth(x, 3) = "A");

	z := s_substr(x, 0, 3);
	assert(eval(s_substr(x, 0, 3)) = "AAA");

	return 1
}
