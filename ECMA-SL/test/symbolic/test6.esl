function main() {
	x := symbolic(__$Str, "x");
  	y := s_len(x);
	assume(y = 4);

	assume(s_substr(x, 0, 3) = "EFG");
	assume(s_nth(x, 3) = "A");

	z := s_substr(x, 0, 3);
	assert(z = "EFG");

	return 1
}
