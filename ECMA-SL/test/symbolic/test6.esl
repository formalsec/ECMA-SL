function main() {
	x := __api_mk_symbolic(__$Str, "x");
  	y := s_len(x);
	__api_assume(y = 4);

	__api_assume(s_substr(x, 0, 3) = "EFG");
	__api_assume(s_nth(x, 3) = "A");

	z := s_substr(x, 0, 3);
	assert(z = "EFG");

	return 1
}
