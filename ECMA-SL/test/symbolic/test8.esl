function main() {
  x := __api_mk_symbolic(__$Int, "x");
  __api_assume(x = 2);
  y := x & 2;

  assert(y = x);
  y := x | 2;

  assert(y = x);
  y := (x ^ 2);
  y := y ^ 2;

  assert(y = x);

  return 1
}
