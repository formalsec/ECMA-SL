function main() {
  x := symbolic(__$Flt, "x");
  assume(x = 2.0);
  y := x & 2.0;

  assert(y = x);
  y := x | 2.0;

  assert(y = x);
  y := (x ^ 2.0);
  y := y ^ 2.0;

  assert(y = x);

  return 1
}
