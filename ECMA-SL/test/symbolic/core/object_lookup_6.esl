function main() {
  y := {};
  x := extern str_symbol("x");
  z := extern str_symbol("z");

  y["length"] := 2.0;
  y[x] := 1.0;
  y[z] := 3.0;

  t := y["length"];
  assert(t = 2.0 || t = 1.0 || t = 3.0);
  return 0
}
