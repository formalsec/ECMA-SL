type numberType := number;
type stringType := string;
type booleanType := boolean;

function foo() {
  return 0
};

type afterFunctionType := number;

function main() {
  numberVar: numberType := 10;
  stringVar: stringType := "abc";
  booleanVar: booleanType := true;
  afterFunctionVar: afterFunctionType := 0;
  return 0
}