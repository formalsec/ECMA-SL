function main() {
  numberVariable: number := 10;
  numberVariableParentherized: (number) := 10;
  numberPair: number * boolean := (10, true);
  numberPairParentherized: (number * boolean) := (10, true);
  differentTypeTuple: number * boolean * string := (10, true, "abc");
  tupleOfTuples: (number * (boolean * string)) := (10, (true, "abc"));
  tupleOfTuples: ((number * boolean) * string) := ((10, true), "abc");
  tupleOfTuples: ((number * boolean) * (string * number)) := ((10, true), ("abc", 10.0));
  return 0
}