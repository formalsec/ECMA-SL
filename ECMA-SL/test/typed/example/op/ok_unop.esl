function neg() {
  foo: number := 10;
  bar: number := -foo;
  return 0
};

function not() {
  foo: boolean := true;
  bar: boolean := !foo;
  return 0
};

function isNaN() {
  foo: number := 10.0;
  bar: boolean := is_NaN foo;
  return 0 
};

function main() {
  return 0
}