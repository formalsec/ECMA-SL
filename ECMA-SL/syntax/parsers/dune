(ocamllex
 (modules lexer eLexer))

(menhir
 (modules op_parser parser)
 (merge_into parser)
 (flags --table --explain))

(menhir
 (modules op_parser eParser)
 (merge_into eParser)
 (flags --table --explain))
