
function main() {
  o := {};
  x := se_mk_symbolic(__$Str, "x");
  y := se_mk_symbolic(__$Str, "y");

  o["key"] := 1;
  o["key2"] := 2;

  se_assume((x = "key") || (x = "key2"));

  print(o[x] + o["key"]);

  print(o[x]);

  assert((o[x] = 1 || o[x] = 2));
  /* assert(o[x] = 1 || o[x] = 2); */
  return 0
}
