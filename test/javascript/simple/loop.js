/**
 * * javaScript/simple/loop.js *
 * 
 * Loop statement that iterates 1000 times and returns the sum produced.
 * 
 * ?Expected result:	("C", 'normal, 499500.0, 'empty)
 * ?Expected prints:	-
*/

let x = 0;
for (let i = 0; i < 1000; i++) {
	x += i;
}
x