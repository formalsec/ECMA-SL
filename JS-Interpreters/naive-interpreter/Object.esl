function Object() {
  object := { 
    __proto__: null
  };
  return object
}; 

function resolve_property(obj, prop_name) {
  if (prop_name in_obj obj) {
    return obj[prop_name]
  } else {
    if (obj.__proto__ = null) {
      return 'undefined
    } else {
      return resolve_property(obj.__proto__, prop_name)
    }
  }
}; 

function update_object(obj_val, prop_name, rhs){
  obj_val[prop_name] := rhs; 
  return
}