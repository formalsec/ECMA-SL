function literal_value(scope, value) {
  print("Literal [value]");
  print(value);
  return value
};

function identifier_value(scope, name) {
  print("Identifier [name][value]:");
  id_data := resolve_id(scope, scope, name, true);
  print(name);
  print(id_data.value);
  return id_data.value
}